========
Overview
========

This is a meta package that pulls in dependencies on each of the new standalone
pytest plugins that are used by the astropy core and affiliated packages. It
is currently WIP.
